# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with
code in this repository.

Project guidance lives in `AGENTS.md`, which is the source of truth. Edit
that file, not this one — the import below pulls its content in here so it
still reaches any tooling that only reads `CLAUDE.md`.

@AGENTS.md
